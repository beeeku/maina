---
title: Roadmap
description: Upcoming features and the future direction of Maina.
---

Maina is open source (Apache 2.0) and actively developed. Here is what is shipped and planned.

## Shipped (v0.2.0)

- **24 CLI commands** across Define, Build, Verify, and Meta phases
- **12-tool verification pipeline** -- Biome, Semgrep, Trivy, Secretlint, SonarQube, Stryker, diff-cover, AI review, slop detection, and visual regression
- **Multi-language support** -- TypeScript, Python, Go, Rust with language-specific profiles
- **Visual verification** -- Playwright screenshot regression testing
- **Workflow context** -- rolling summary forwarded between lifecycle steps
- **Background RL** -- async feedback recording at each workflow step with post-workflow analysis
- **Interactive configuration** -- `maina configure` for constitution, conventions, tools, visual URLs
- **Verification proof** -- `maina pr` includes verification artifacts in the PR body
- **Host delegation** -- works inside Claude Code, Codex, and OpenCode via structured DelegationPrompt
- **8 MCP tools** for IDE integration
- **5 cross-platform skills** for Claude Code, Cursor, Codex, Gemini CLI
- **990 tests**

## v0.3.0 -- Enterprise Languages

- **C# (.NET) support** -- `dotnet format` / Roslyn analyzers, `.sln`/`.csproj` detection
- **Java/Kotlin support** -- Checkstyle, SpotBugs, PMD, `pom.xml`/`build.gradle` detection
- **PHP support** -- PHPStan, Psalm
- **Per-file language detection** in polyglot repos (currently uses primary language)

## v0.4.0 -- Structured Output + CI

- **`--json` flag** on all commands for machine-readable output
- **CI integration** -- `maina verify --json` for GitHub Actions, GitLab CI
- **Exit codes** -- meaningful exit codes for scripting
- **DAST with ZAP** -- Dynamic Application Security Testing for web applications
- **Lighthouse** -- Performance, accessibility, and SEO auditing

## v1.0.0 -- Team / Cloud

- **Team sync (Maina Cloud)** -- Shared prompts, feedback, and learned preferences across team members via Turso
- **Usage dashboard** -- Track verification metrics across the team
- **SSO** -- Single sign-on for teams
- **Shared visual baselines** -- S3/CDN storage for visual regression baselines

## v2.0.0 -- Enterprise

- **Air-gapped deployment** -- On-premise with audit logging, SOC 2 compliance
- **Custom model fine-tuning** -- From your team's accepted reviews and feedback
- **Chrome extension** -- Review AI-generated code in GitHub PRs
- **`transformContext` hooks** -- Programmatic context manipulation
- **Self-extending agent** -- Maina proposes its own extensions based on usage patterns

## Contributing

Maina is open source and welcomes contributions.

### Getting started

```bash
git clone https://github.com/beeeku/maina
cd maina
bun install
bun run build
bun run test
```

### Development commands

```bash
bun run dev          # Dev mode with watch
bun run check        # Biome lint + format check
bun run typecheck    # tsc --noEmit
bun run verify       # Full verification: check + typecheck + test
```

### Conventions

- **Conventional commits** with scopes: `cli`, `core`, `mcp`, `skills`, `docs`, `ci`
- **TDD always** -- write tests first, watch them fail, implement, watch them pass
- **Result pattern** -- `Result<T, E>` for error handling, never throw
- **Bun only** -- Bun runtime, bun:test, bun:sqlite. Not Node.js, not Jest, not Vitest.

### Where to contribute

- **New language profiles** -- Add a `LanguageProfile` in `packages/core/src/language/profile.ts`
- **New analysis tools** -- Add a tool adapter in `packages/core/src/verify/`
- **Linter parsers** -- Add output parsers in `packages/core/src/verify/linters/`
- **Skills** -- Write new SKILL.md files in `packages/skills/`
- **Documentation** -- Improve these docs in `packages/docs/`
- **Bug reports and feature requests** -- Open a GitHub Issue

### Dogfooding

Every commit to Maina goes through `maina commit`. If you contribute, use `maina commit` for your commits too. If a gate is noisy, fix the gate -- do not skip it.
