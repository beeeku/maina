pre-commit:
  parallel: true
  commands:
    biome:
      run: bunx @biomejs/biome check .
    typecheck:
      run: bun run --filter '*' typecheck

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
